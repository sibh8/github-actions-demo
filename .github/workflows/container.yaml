name: Containers
on:
- push

jobs:
  containers:
    runs-on: ubuntu-latest
    container:
      image: node:14-alpine
    steps:
    - name: Get node version
      run: |
        node -v
        cat /etc/os-release